

## Rename "Weights Trends" to "Exercise Trends"

A single text change in `src/pages/Trends.tsx` -- update the heading from "Weights Trends" to "Exercise Trends".

### Technical detail

Update the string in the `<h2>` element that reads "Weights Trends" to "Exercise Trends" in `src/pages/Trends.tsx`.

