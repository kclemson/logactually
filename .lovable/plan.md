

# Feedback System Enhancements — COMPLETED

All items implemented:

1. ✅ Header renamed "Help" → "Help & Feedback"
2. ✅ Database: `feedback_id` (auto-increment from 1000) and `resolved_reason` (text) columns added
3. ✅ Types updated with new fields
4. ✅ Queries updated to select new fields
5. ✅ Resolve mutation supports optional `reason` param
6. ✅ Admin UI: shows `#ID`, "Resolve Fixed" green link, "Fixed" badge on resolved items
7. ✅ User feedback UI: collapsible rows with truncated preview, reply on open items, differentiated resolved status
