Change the summary span in `BloodworkPanelGroup.tsx` (the new "{N} results: …" line) from `text-sm` (foreground) to `text-xs text-muted-foreground` so it matches the previous gray result-count styling. Leave the pending/failed fallback label as-is.