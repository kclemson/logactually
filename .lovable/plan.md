Remove the "Up to 3 at a time" sublabel from the bloodwork upload button's loading state in `src/components/BloodworkUploadInput.tsx`. The spinner + "Reading your documents…" line already communicates progress. The per-row Queued / Reading & parsing states make the actual throughput visible.