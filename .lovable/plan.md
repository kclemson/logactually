
# Calorie target tooltip improvements — COMPLETED

All 4 items implemented:

1. ✅ **Dot opacity fix** — dots in header lines now have `opacity-100` to match legend brightness
2. ✅ **Interleaved legends** — daily legend above daily section, weekly legend above weekly section
3. ✅ **Rollup tooltip aligned** — uses same 4-column grid legend format with "rolling:" label
4. ✅ **Threshold constants extracted** — `DAILY_GREEN_MAX`, `DAILY_AMBER_MAX`, `ROLLUP_GREEN_MAX`, `ROLLUP_AMBER_MAX` in `calorie-target.ts`
